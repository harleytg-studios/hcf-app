name: Apply Widget More Settings

on:
  push:
    branches: [dev]
    paths:
      - '.github/scripts/apply-widget-more-settings.py'
      - '.github/workflows/apply-widget-more-settings.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Dev
        uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0

      - name: Apply expanded widget settings patch
        run: python3 .github/scripts/apply-widget-more-settings.py

      - name: Commit patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add 'source code/src/com/harleytg/forum/HcfCore.java' \
                  'source code/src/com/harleytg/forum/HcfUI.java' \
                  'source code/src/com/harleytg/forum/HcfWidget.java' \
                  'source code/res/layout/widget_hcf_notifications.xml' \
                  'README.md' \
                  '.github/scripts/verify-release-readiness.py'
          git diff --cached --quiet && exit 0
          git commit -m 'Expand HCF widget settings'
          git push origin dev
