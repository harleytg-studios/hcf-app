Harley's Clan Forum v0.4.17d

Fixed — Logs & Diagnostics clipping
- Filter chips now use two fixed rows and stay fully visible on narrow screens.
- Logs open newest-first instead of auto-scrolling into the middle of an entry.
- Long forum URLs and route names wrap at safer points.
- Monospace log text no longer uses automatic hyphenation.
- Grouped entries are ordered by their newest occurrence.

Preserved
- Search, severity/WebView/Network filters, grouped events, Diagnostics, Copy, Export and Clear.
