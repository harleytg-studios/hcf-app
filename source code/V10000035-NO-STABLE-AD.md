# Beta/Dev v10000035 — No Stable/Main Promotion

- Package remains `com.harleytg.forum.dev`.
- Android `versionCode` is `10000035`.
- Android system-visible `versionName` is `1.0 (10000035)` so App info shows the numeric build code.
- In-app public release version remains `v1.0`.
- Stable/Main promotional and installer UI remains excluded from Beta/Dev.
- No Stable package recommendation or Stable install prompt is included.
- Development/Beta update channel remains unchanged.
- Build script uses the Beta v2 signing identity.
